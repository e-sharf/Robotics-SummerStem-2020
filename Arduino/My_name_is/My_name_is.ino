int num; //declaring num as an int type
void setup() {
  // put your setup code here, to run once:
  Serial.begin(9600); //notes after the slashes
}
  //  int r = random(1,11);

  //if ((r < 3) || (r >=8))
    //Serial.println (r);
    //delay (100);

  for (int i=0; i<6; i++) {
  }
}
void loop() {
  // put your main code here, to run repeatdly
  
