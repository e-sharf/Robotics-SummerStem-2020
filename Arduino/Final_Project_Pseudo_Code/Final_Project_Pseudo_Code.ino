//global variables
//servo variables
//SODAR initialization
//distance variable array
//int distance[];

void setup() {
  //Same code
}

void loop() {

  //if (sensor.isFinished)())
{
      //input = sensor.getRange
      //if distance < 50
        //raw_motor)control(reduced amount)
        //obstacle aoidance();
      //sensor.start();
}
  //rest of normal code
}

  void obstacle _avoidance()}
    //make the servo look left and right
    //if left distance< right distance, turn right (control_state.setRotationState(33))
    //else, turn left. (control_state.setRotationState(33))
    //while distance < 35
      //delay 3 seconds
}
